# Use a devnet/mainnet RPC URL; for devnet use https://api.devnet.solana.com
RPC_URL=https://api.devnet.solana.com
# PRIVATE_KEY can be a JSON array (serialized secret key) OR a base58 encoded keypair
PRIVATE_KEY_JSON=[0,1,2,...]
# or
# PRIVATE_KEY_BASE58=YourBase58EncodedSecretKey

# Slippage tolerance (in percent)
SLIPPAGE_PERCENT=0.5

# Minimal profit threshold (in USD or quote units) for executing arbitrage
MIN_PROFIT_USD=0.5
